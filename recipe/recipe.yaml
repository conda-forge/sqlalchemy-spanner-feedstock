schema_version: 1

context:
  name: sqlalchemy-spanner
  version: "1.20.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/sqlalchemy_spanner-${{ version }}.tar.gz
  sha256: 4e5226c75e2537cb4ec0a0c82b0fb1dcac485dc00d3b025db7ff88cb9759f35b

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - sqlalchemy >=1.1.13
    - google-cloud-spanner >=3.55.0
    - alembic

tests:
  - python:
      imports:
        - google.cloud.sqlalchemy_spanner
      python_version:
        - ${{ python_min }}.*
        - "*"
  - requirements:
      run:
        - python ${{ python_min }}.*
        - pip
    script:
      - pip check

about:
  summary: SQLAlchemy dialect integrated into Cloud Spanner database
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/googleapis/google-cloud-python/tree/main/packages/sqlalchemy-spanner

extra:
  recipe-maintainers:
    - xylar
